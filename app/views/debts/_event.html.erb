<%# locals: (event:, highlight: false) %>

<li>
  <pre class="text-wrap border p-3 bg-gray-100 text-gray-500 border-gray-400 [&.highlighted]:bg-orange-400"
       data-controller="<%= "highlight" if highlight %>"><%= event.inspect %></pre>
</li>
